# Real credentials and runtime state (kept server-side, never committed)
lottery-bot.env
*.local-overrides.env
data/
bin/
dist/

# OS noise
.DS_Store

# AI tool session artifacts
.omx/
.superpowers/
.workbuddy/

# 生产发布脚本仅存本机，不入公共仓库
/deploy/release.sh
